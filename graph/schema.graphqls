type Pet {
  id: ID!
  name: String!
  points: Int!
}

type Query {
  getPet(id: ID!): Pet
  listPets: [Pet!]!
}

type Mutation {
  createPet(name: String!): Pet!
  updatePetPoints(id: ID!, points: Int!): Pet!
}
